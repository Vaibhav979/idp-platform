# Fix 405 SUCCESSFUL - App Restarted Fresh

## Status
- Port conflict resolved (stopped old instance).
- Fresh build/run with latest code (includes @GetMapping("/{id}").

## Completed Steps:
1. [x] Stopped old app.
2. [x] Restart `.\mvnw.cmd clean spring-boot:run` (building/running).
3. [ ] Restart: `.\mvnw.cmd clean spring-boot:run`
4. [ ] Test flow (once 'Started IdpApplication'):
   - Create: `curl -X POST http://localhost:8080/projects -H "Content-Type: application/json" -d "{\"name\":\"test\",\"repoUrl\":\"git://test\"}"`
   - Trigger: `curl -X POST http://localhost:8080/deployments/projects/1`
   - Get: `curl http://localhost:8080/deployments/1`
5. [ ] Complete: Endpoint now works post-rebuild.
