# Fix 405 on GET /deployments/1\n\n## Steps:\n\n1. [ ] Stop any running Spring Boot app.\n2. [ ] Run `mvnw clean spring-boot:run` to rebuild and start.\n3. [ ] Test flow:\n   - Create project: `curl -X POST http://localhost:8080/projects -H \"Content-Type: application/json\" -d \"{\\\"name\\\":\\\"test\\\",\\\"repoUrl\\\":\\\"git://test\\\"}\"`\n   - Get projectId (e.g. 1)\n   - Trigger deployment: `curl -X POST http://localhost:8080/deployments/projects/<projectId>`\n   - Get deployment: `curl http://localhost:8080/deployments/1`\n4. [ ] If 405 persists, add logging to controller and rebuild.\n5. [ ] Mark complete.
