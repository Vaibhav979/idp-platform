# TODO: Fix 405 Method Not Allowed on GET /deployments/1

## Steps:
1. [ ] Restart Spring Boot app to load latest DeploymentController: `mvnw spring-boot:run`
2. [ ] Verify Deployment exists: Visit http://localhost:8080/h2-console (JDBC: jdbc:h2:mem:idp-db; user:sa; pass:empty), check DEPLOYMENT table for ID=1.
3. [ ] Test pure GET request: `curl -v http://localhost:8080/deployments/1` (expect 404 if no Deployment, confirming GET works).
4. [ ] If 405 persists, check app startup logs for request mappings (look for /deployments/**).
5. [ ] Trigger deployment if needed: POST /deployments/projects/1 via curl/Postman.
6. [ ] [DONE] Test browser access again.

**Expected: After restart, GET works (404 -> success if data present).**
