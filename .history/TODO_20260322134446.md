# Fix 405 on GET /deployments/1 - APP STARTING

## Steps:

1. [x] Stop any running Spring Boot app.
2. [x] Run `.\mvnw.cmd clean compile spring-boot:run` to rebuild and start (running, app starting).
3. [ ] Wait for full startup (look for 'Started IdpApplication' and Tomcat 8080), then test flow in NEW TERMINAL:
   - Create project: `curl -X POST http://localhost:8080/projects -H \"Content-Type: application/json\" -d \"{\\\"name\\\":\\\"test\\\",\\\"repoUrl\\\":\\\"git://test\\\"}\"`
   - Note projectId from response (likely 1)
   - Trigger deployment: `curl -X POST http://localhost:8080/deployments/projects/1`
   - Get deployment: `curl http://localhost:8080/deployments/1` (should return JSON, not 405)
4. [ ] If 405, investigate logs/add logging.
5. [x] Mark complete once fixed.
